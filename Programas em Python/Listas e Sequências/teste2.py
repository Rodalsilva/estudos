import programa_3_5

L = programa_3_5.progressao_aritmetica("Digite um valor para o primeiro termo:",
                                       "Digite um valor para a razão de progressão:",
                                       "Digite um inteiro n > 1 para o número de termos:")
v = float(input("Digite um número a pesquisar: "))
def pesquisa_PA(L, v):
    x = 0
    for e in L:
        if e != v:
            if x >= len(L)-1:
                return "Elemento não encontrado."
        else:
            e == L[x]
            return x + 1
            break
        x += 1






    n = programa_3_6f.pesquisa_PA(L, v)
    m = int(n)
    x = 0
    while x < m:
        x = y + 2
        L.append(0)
        L[y+1] = L[y]+R
    x + 1
print(n)
        
